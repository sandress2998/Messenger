eureka:
  client:
    service-url:
      defaultZone: http://discovery-service:8761/eureka

spring:
  application:
    name: websocket-service
  data:
    redis:
      host: websocket-redis
      port: 6380
  kafka:
    bootstrap-servers: kafka:9092

server:
  port:
    8092

my:
  security:
    jwt-timeout-in-minutes: 60