spring:
  profiles:
    active: dev
  application:
    name: user-service
  cloud:
    config:
      uri: http://config-service:8000
      fail-fast: true
      allow-override: true

management:
  endpoints:
    web:
      exposure:
        include: refresh

logging:
  level:
    org.springframework.cloud: DEBUG
